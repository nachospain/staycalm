<%= month_calendar events: @tasks do |date, tasks| %>
  <%= date.day %>

  <% tasks.each do |task| %>
    <ul>
      <li><%= link_to task.name, task_path(task.id) %></li>
    </ul>
  <% end %>
<% end %>
